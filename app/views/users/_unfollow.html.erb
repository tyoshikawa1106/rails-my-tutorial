<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <div style="width: 100px;">
    <%= button_tag( class: "btn btn-default btn-sm") do %>
      <%= content_tag :span, " Unfollow", class: "s1utility s1utility-error" %>
    <% end %>
  </div>
<% end %>